import 'package:flutter/material.dart';

class MySqurare extends StatelessWidget {
  const MySqurare({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
